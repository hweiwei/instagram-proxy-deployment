# Instagram Proxy 配置文件
# 复制此文件为 env.conf 并修改配置

# 服务器公网 IP（必填）
SERVER_IP=216.128.157.113

# WebSocket 路径（可选，默认 /instagram-ws-随机字符）
WS_PATH=/instagram-ws

# 域名（可选，用于 CDN 模式）
# DOMAIN=your-domain.com
# USE_DOMAIN=false
